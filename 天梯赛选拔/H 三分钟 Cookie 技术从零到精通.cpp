#include <iostream>
using namespace std;

int need[1005] = {0};
int has[1005] = {0};

int main() {
    int n, k;
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        cin >> need[i];
    }
    for (int i = 0; i < n; i++) {
        cin >> has[i];
    }

    for (int i = )
}
